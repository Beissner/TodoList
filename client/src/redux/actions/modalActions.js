
import { TOGGLE_MODAL } from './types';

export const toggleModal = (value) => ({ type: TOGGLE_MODAL, payload: value });
//export const toggleProjectModal = (value) => ({ type: PROJECT_MODAL, payload: value });